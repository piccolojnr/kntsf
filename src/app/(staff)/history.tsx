import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function StaffHistoryScreen() {
  return (
    <PlaceholderScreen
      title="Staff History"
      description="Review recent staff scan attempts, outcomes, and verification history."
      screenName="staff/history"
    />
  );
}
