import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function OperationsScanScreen() {
  return (
    <PlaceholderScreen
      title="Operations Scan"
      description="Use the primary operations workflow to scan and verify student access records."
      screenName="operations/scan"
    />
  );
}
