import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function AdminPermitsScreen() {
  return (
    <PlaceholderScreen
      title="Admin Permits"
      description="Review, track, and manage permit records across the application."
      screenName="admin/permits"
    />
  );
}
